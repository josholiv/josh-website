---
const { platform, username, displayName = platform } = Astro.props;
---
<a href={`https://${platform}/${username}`}>{displayName}</a>

<style>
    a {
      padding: 0.5rem 1rem;
      color: white;
      background-color: #4c1d95;
      text-decoration: none;
    }
  </style>